#include <stdio.h>
int main(void)
{
	int height, age;

	pritnf("키와 나이를 입력하세요");
	scanf("%d %d", &height, &age);
	if (height >= 140 && age >= 10)
		printf("타도 좋습니다.");

	else
		printf("죄송합니다.");
















	return 0;
}
